# Demo runtime state: synthetic PKI, keys, catalog records, logs and pid files.
# Generated by up.sh, removed by down.sh. Never commit it.
.state/
# Optional overrides for remote mode (identifiers only, never keys).
remote.env
# Build output of up.sh (only kept when a build fails).
.build.log
