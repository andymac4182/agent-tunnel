# shellcheck shell=bash
# Feature plug-in: Adapters: the TypeScript client and native adapters calling the device
# TODO(adapters feature PR): fill this in per scripts/demo/features/_template.sh,
# set FEATURE_READY=1, and add docs/demo/adapters.md. Until then show.sh lists it
# as not available and up.sh skips it.
FEATURE_NAME=adapters
FEATURE_TITLE="Adapters: the TypeScript client and native adapters calling the device"
FEATURE_READY=0
FEATURE_TODO='TODO(adapters feature PR): TypeScript client/adapters using TOKEN=$(scripts/demo/token.sh SCOPE)'
FEATURE_REMOTE=0
