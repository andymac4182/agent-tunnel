# shellcheck shell=bash
# Feature plug-in: ACP: a coding agent on the device, driven through the relay
# TODO(acp feature PR): fill this in per scripts/demo/features/_template.sh,
# set FEATURE_READY=1, and add docs/demo/acp.md. Until then show.sh lists it
# as not available and up.sh skips it.
FEATURE_NAME=acp
FEATURE_TITLE="ACP: a coding agent on the device, driven through the relay"
FEATURE_READY=0
FEATURE_TODO='TODO(acp feature PR): acp-http-v1 service, fixture or real agent, HTTP/2 client steps'
FEATURE_REMOTE=0
