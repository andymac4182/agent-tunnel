# shellcheck shell=bash
# Feature plug-in: Computer use: a synthetic desktop driven through the relay
# TODO(cua feature PR): fill this in per scripts/demo/features/_template.sh,
# set FEATURE_READY=1, and add docs/demo/cua.md. Until then show.sh lists it
# as not available and up.sh skips it.
FEATURE_NAME=cua
FEATURE_TITLE="Computer use: a synthetic desktop driven through the relay"
FEATURE_READY=0
FEATURE_TODO='TODO(cua feature PR): CUA export against a dedicated VM or the synthetic fixture, never the presenter desktop'
FEATURE_REMOTE=0
