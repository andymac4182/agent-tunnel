{
  "name": "@agent-tunnel/client",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript filesystem client for the agent-tunnel.9p.v1 profile: descriptor, authenticated WSS, 9P2000.L codec, session, bytes and limits. The four native framework adapters are separate packages and do not exist yet.",
  "license": "SEE LICENSE IN ../../LICENSE",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./ninep": "./src/ninep/codec.ts"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "test": "node --test --test-reporter=spec \"test/**/*.test.ts\"",
    "typecheck": "tsc --project tsconfig.json",
    "fuzz:generate": "node fuzz/generate.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "24.10.1",
    "typescript": "5.9.3"
  }
}
