Read-only probe of a `cua-macos-golden-denied` clone at the default `1280x800pt` display, which came up as 1024x768 points at backing scale 2.0. Run `probe --expect denied`, exit 0. `run.txt` carries the nonce and `head=62e0daa1`. `probe.py`'s `numbers_only` recording of `get_desktop_state` on unverified frames was uncommitted at that head; it is committed in the next commit on this branch. Server logs and `known_hosts` are omitted. No screenshot is kept, because no frame was the fixture.
