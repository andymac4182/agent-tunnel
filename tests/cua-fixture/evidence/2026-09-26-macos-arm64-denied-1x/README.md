Read-only probe of a `cua-macos-golden-denied` clone created with `--display 1280x800px` (1280x800 points at scale 1.0), in a full `cycle --expect denied`, exit 0. `run.txt` carries the nonce and `head=62e0daa1`. `probe.py`'s `numbers_only` recording of `get_desktop_state` on unverified frames was uncommitted at that head; it is committed in the next commit on this branch. Server logs and `known_hosts` are omitted. No screenshot is kept, because no frame was the fixture.
