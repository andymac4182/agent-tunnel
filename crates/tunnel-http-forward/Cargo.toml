[package]
name = "tunnel-http-forward"
description = "Pure bounded http-forward/1 record codec, strict heads, and directional state"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

# Deliberately dependency-free: head JSON is hand-parsed so duplicate keys,
# lone surrogates and trailing data are rejected by construction.

[lints]
workspace = true
